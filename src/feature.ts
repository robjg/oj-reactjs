/**
 * Allow featrues of the UI to be turned on and off. Currently hard coded but hopefully
 * implemented in a way that would allow preferences to be changed at login for instance.
 */


export class FeatureControl {

    static readonly contextMenuEnabled: boolean = false;

    static readonly dndEnabled: boolean = false;

}